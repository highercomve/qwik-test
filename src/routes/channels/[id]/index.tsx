import { component$ } from "@builder.io/qwik";

export const Channels = component$(() => {

  return (
    <div id="channels-list">
      <header>
        <h1>Channels</h1>
      </header>
    </div>
  )
})